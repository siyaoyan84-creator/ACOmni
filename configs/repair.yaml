base_variant: acomni_affective
max_steps: 50
selected_checkpoint_step: 20
learning_rate: 5.0e-8
seed: 42
reward_functions:
  - format
  - accuracy
  - affective_context
  - emotion_consistency
reward_weights:
  affective_context: 0.02
  emotion_consistency: 0.01
